def calculateSum(numbers):
    sum = 0
    for num in numbers:
        sum += num
    return sum


def calculateProduct(numbers):
    result = 1
    for number in numbers:
        result *= number
    return result
